
  <% resorts.each do |resort| %>
    <%= link_to resort_path(resort.id) do %>

      <div>
        <%= image_tag resort.image, size: '200x120' %>
      </div>
      <p><strong><%= resort.name %></strong></p>
      <p><strong><%= resort.feature %></strong></p>

    <% end %>
  <% end %>
